@page
@model KoiServiceCenter.WebApp.Pages.Admin.vetschedule.DeleteModel

<html lang="en">
<head>
    <title>Delete Vet Schedule</title>
    <style>
        .page-content {
            max-width: 600px;
            margin: auto;
            padding-top: 30px;
        }

        .title-bordered {
            background-color: #343a40;
            color: white;
            text-align: center;
            padding: 10px;
            border-radius: 5px;
            font-weight: bold;
        }

        .warning-text {
            color: red;
            font-size: small;
            text-align: center;
            margin-top: 10px;
        }

        .card {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .card-body {
            padding: 15px;
        }

        .button-group {
            display: flex;
            gap: 15px; 
            justify-content: flex-start; 
            margin-top: 15px;
        }

        .btn {
            padding: 8px 16px;
            font-size: 14px;
        }

        .btn-danger {
            background-color: #dc3545;
            color: white;
        }

        .btn-secondary {
            background-color: #6c757d;
            color: white;
        }

        .btn:hover {
            opacity: 0.8;
        }
    </style>
</head>
<body>

    <div class="page-content">
        <h4 class="title-bordered">Xác nhận xóa lịch bác sĩ thú y</h4>
        <p class="warning-text">
            Bạn có chắc chắn muốn xóa lịch hẹn này không? Thao tác này không thể hoàn tác!
        </p>
        <hr />

        <div class="card shadow-sm mb-3">
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Ngày hẹn</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.VetSchedule.ScheduleDate)</dd>

                    <dt class="col-sm-4">Khung giờ</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.VetSchedule.TimeSlot)</dd>

                    <dt class="col-sm-4">Bác sĩ thú y</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.VetSchedule.Veterinarian.UserName)</dd>
                </dl>
            </div>
        </div>

        <form method="post">
            <input type="hidden" asp-for="VetSchedule.ScheduleId" />
            <div class="button-group">
                <input type="submit" value="Xóa" class="btn btn-danger" />
                <a class="btn btn-secondary" asp-page="./Index">Quay lại danh sách</a>
            </div>
        </form>
    </div>

</body>
</html>
