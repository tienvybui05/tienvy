@page
@model KoiServiceCenter.WebApp.Pages.Admin.servicehistory.DeleteModel

<html lang="en">
<head>
    <title>Delete Service History</title>
    <style>
        .page-content {
            max-width: 600px;
            margin: auto;
            padding-top: 30px;
        }

        .title-bordered {
            background-color: #343a40;
            color: white;
            text-align: center;
            padding: 10px;
            border-radius: 5px;
            font-weight: bold;
        }

        .button-group {
            display: flex;
            gap: 15px;
            justify-content: flex-start;
            margin-top: 15px;
        }

        .card {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .card-body {
            padding: 15px;
        }

        .warning-text {
            color: red;
            font-size: small;
            text-align: center;
            margin-top: 10px;
        }
    </style>
</head>
<body>

    <div class="page-content">
        <h4 class="title-bordered">Xác nhận xóa lịch sử dịch vụ</h4>
        <p class="warning-text">
            Bạn có chắc chắn muốn xóa lịch sử dịch vụ này không? Thao tác này không thể hoàn tác!
        </p>
        <hr />

        <div class="card shadow-sm mb-3">
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Ngày dịch vụ</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ServiceHistory.ServiceDate)</dd>

                    <dt class="col-sm-4">Kết quả</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ServiceHistory.Result)</dd>

                    <dt class="col-sm-4">Khách hàng</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ServiceHistory.Customer.FullName)</dd>

                    <dt class="col-sm-4">Dịch vụ</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ServiceHistory.Service.Description)</dd>

                    <dt class="col-sm-4">Bác sĩ thú y</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.ServiceHistory.Veterinarian.Email)</dd>
                </dl>
            </div>
        </div>

        <form method="post">
            <input type="hidden" asp-for="ServiceHistory.HistoryId" />
            <div class="button-group">
                <input type="submit" value="Xóa" class="btn btn-danger" />
                <a class="btn btn-secondary" asp-page="./Index">Quay lại danh sách</a>
            </div>
        </form>
    </div>

</body>
</html>
