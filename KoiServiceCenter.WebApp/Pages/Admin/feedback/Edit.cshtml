@page
@model KoiServiceCenter.WebApp.Pages.Admin.feedback.EditModel

<h4 class="mb-4 text-primary">Đánh giá</h4>
<hr />
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <form method="post" class="shadow-lg p-4 rounded bg-white border">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <input type="hidden" asp-for="Feedback.FeedbackId" />

                <div class="form-group mb-3">
                    <label asp-for="Feedback.Customer" class="control-label text-secondary"></label>
                    <select asp-for="Feedback.CustomerId" class="form-control bg-light" asp-items="ViewBag.CustomerId"></select>
                    <span asp-validation-for="Feedback.Customer" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Feedback.Service" class="control-label text-secondary"></label>
                    <select asp-for="Feedback.ServiceId" class="form-control bg-light" asp-items="ViewBag.ServiceId"></select>
                    <span asp-validation-for="Feedback.Service" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Feedback.Rating" class="control-label text-secondary"></label>
                    <input asp-for="Feedback.Rating" class="form-control bg-light" />
                    <span asp-validation-for="Feedback.Rating" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Feedback.Comments" class="control-label text-secondary"></label>
                    <input asp-for="Feedback.Comments" class="form-control bg-light" />
                    <span asp-validation-for="Feedback.Comments" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between align-items-center mt-4">
                    <a asp-page="./Index" class="btn btn-outline-secondary rounded-pill px-3 py-2 transition-link">Quay lại</a>
                    <input type="submit" value="Lưu" class="btn btn-primary rounded-pill px-4 py-2 shadow-sm transition-button" />
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
