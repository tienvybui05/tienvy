@page
@model KoiServiceCenter.WebApp.Pages.Admin.feedback.DetailsModel

<html lang="en">
<head>
    <title>Chi tiết Đánh giá</title>
    <style>
        .page-content {
            max-width: 600px;
            margin: auto;
            padding-top: 30px;
        }

        .title-bordered {
            background-color: black;
            color: white;
            text-align: center;
            padding: 10px;
            border-radius: 5px;
            font-weight: bold;
        }

        .button-group {
            display: inline-flex;
            gap: 10px;
        }
    </style>
</head>
<body>

    <div class="page-content">
        <h4 class="title-bordered">Chi tiết Đánh giá</h4>
        <hr />

        <div class="card shadow-sm mb-3">
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Xếp hạng</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Feedback.Rating)</dd>

                    <dt class="col-sm-4">Bình luận</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Feedback.Comments)</dd>

                    <dt class="col-sm-4">Khách hàng</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Feedback.Customer.FullName)</dd>

                    <dt class="col-sm-4">Dịch vụ</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Feedback.Service.Description)</dd>
                </dl>
            </div>
        </div>

        <div class="button-group">
            <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@Model.Feedback.FeedbackId">Sửa</a>
            <a class="btn btn-secondary" asp-page="./Index">Quay lại</a>
        </div>
    </div>

</body>
</html>
