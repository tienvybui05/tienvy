@page
@model KoiServiceCenter.WebApp.Pages.Admin.useraccount.DeleteModel

<html lang="en">
<head>
    <title>Delete User Account</title>
    <style>
        .page-content {
            max-width: 600px;
            margin: auto;
            padding-top: 30px;
        }

        .title-bordered {
            background-color: black;
            color: white;
            text-align: center;
            padding: 10px;
            border-radius: 5px;
            font-weight: bold;
        }

        .button-group {
            display: inline-flex;
            gap: 10px;
        }

        .card {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 1rem;
            border: 1px solid #ddd;
        }

        .card-body {
            padding: 15px;
        }

        .warning-text {
            color: red;
            font-size: small;
            text-align: center;
            margin-top: 10px;
        }
    </style>
</head>
<body>

    <div class="page-content">
        <h4 class="title-bordered">Xác nhận xóa tài khoản</h4>
        <p class="warning-text">
            Việc xóa tài khoản này cũng sẽ xóa các thông tin liên quan của nó ở các bảng khác! Bạn chắc chứ!
        </p>
        <hr />

        <div class="card shadow-sm mb-3">
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Username</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.UserAccount.UserName)</dd>

                    <dt class="col-sm-4">Password</dt>
                    <dd class="col-sm-8">
                        <span id="password" class="text-muted">●●●●●●</span>
                        <button type="button" class="btn btn-link btn-sm" onclick="togglePassword()">
                            <i id="toggleIcon" class="fas fa-eye"></i>
                        </button>
                    </dd>

                    <dt class="col-sm-4">Email</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.UserAccount.Email)</dd>

                    <dt class="col-sm-4">Role</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.UserAccount.Role)</dd>
                </dl>
            </div>
        </div>

        <form method="post">
            <input type="hidden" asp-for="UserAccount.UserId" />
            <div class="button-group">
                <input type="submit" value="Xóa" class="btn btn-danger" />
                <a class="btn btn-secondary" asp-page="./Index">Quay lại</a>
            </div>
        </form>
    </div>

    <script>
        // Show/hide password function with icon toggle
        function togglePassword() {
            const passwordElement = document.getElementById("password");
            const toggleIcon = document.getElementById("toggleIcon");

            if (passwordElement.textContent === "●●●●●●") {
                passwordElement.textContent = "@Model.UserAccount.Password";
                toggleIcon.classList.remove("fa-eye");
                toggleIcon.classList.add("fa-eye-slash");
            } else {
                passwordElement.textContent = "●●●●●●";
                toggleIcon.classList.remove("fa-eye-slash");
                toggleIcon.classList.add("fa-eye");
            }
        }
    </script>

</body>
</html>
